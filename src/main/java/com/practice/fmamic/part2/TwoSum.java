package com.practice.fmamic.part2;

import java.util.HashSet;
import java.util.Set;

class TwoSum {

    int calculate(final long[] input) {

        final Set<Long> set = new HashSet<>();

        for (long inputNumber : input) {
            set.add(inputNumber);
        }

        return 0;
    }
}
